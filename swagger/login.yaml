tags:
  - name: Autenticación
    description: Endpoints de login y control de sesión

paths:
  /login:
    post:
      summary: Iniciar sesión
      consumes:
        - application/x-www-form-urlencoded
      parameters:
        - name: usuario
          in: formData
          required: true
          type: string
        - name: clave
          in: formData
          required: true
          type: string
      responses:
        200:
          description: Login exitoso
        401:
          description: Credenciales inválidas
